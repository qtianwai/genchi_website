# 部署清单

## 1. Supabase 数据库更新

需要在 Supabase SQL Editor 中执行以下 SQL 函数：

```sql
-- 新增：获取店铺关联的视频信息
-- 用于在店铺详情页显示该店铺相关的抖音视频列表

create or replace function get_videos_by_restaurant(p_restaurant_id uuid)
returns table (
  video_id text,
  author_id uuid,
  author_name text,
  author_avatar_url text,
  created_at timestamptz
) as $$
begin
  return query
  select
    ar.video_id,
    a.id as author_id,
    a.name as author_name,
    a.avatar_url as author_avatar_url,
    ar.created_at
  from author_restaurants ar
  join authors a on ar.author_id = a.id
  where ar.restaurant_id = p_restaurant_id
    and ar.video_id is not null
    and ar.video_id != ''
  order by ar.created_at desc;
end;
$$ language plpgsql;
```

### 执行步骤：
1. 打开 [Supabase 控制台](https://supabase.com)
2. 进入项目 → 左侧菜单「SQL Editor」
3. 粘贴上述 SQL 代码
4. 点击「Run」执行

## 2. Railway 后端部署

代码已推送到 GitHub，Railway 会自动检测并重新部署。

### 检查部署状态：
1. 打开 [Railway 控制台](https://railway.app)
2. 进入项目 → 查看「Deployments」
3. 等待部署完成（通常 2-3 分钟）
4. 部署成功后，访问 `https://你的域名.railway.app/docs` 验证新接口

### 新增接口验证：
- `GET /api/restaurants/{restaurant_id}/videos` - 获取店铺关联视频

## 3. iOS App 更新

### 需要在 Xcode 中操作：

1. **添加新文件到项目**：
   - 将 `ios/FoodMap/FoodMap/Services/LocationManager.swift` 拖入 Xcode 项目
   - 确保文件在 Target Membership 中勾选了 FoodMap

2. **更新 Info.plist**：
   - 打开 `ios/FoodMap/genchi/genchi/Info.plist`
   - 确认已添加定位权限说明：
     ```xml
     <key>NSLocationWhenInUseUsageDescription</key>
     <string>需要获取您的位置以显示附近的美食推荐</string>
     ```

3. **编译运行**：
   - 连接 iPhone 或使用模拟器
   - 点击 Run（⌘R）
   - 首次运行时会弹出定位权限请求，点击「允许」

## 4. 功能测试清单

### 用户定位功能
- [ ] 打开 App 后弹出定位权限请求
- [ ] 允许定位后，地图自动移动到用户当前位置
- [ ] 地图上显示蓝色用户位置标记

### 店铺详情弹框
- [ ] 点击地图上的博主头像标记，底部弹出店铺详情卡片
- [ ] 粘贴链接按钮不与详情卡片重合（动态调整位置）
- [ ] 详情卡片中显示关联视频列表（横向滚动）
- [ ] 点击视频卡片可跳转到抖音 App 查看原视频

### 导航功能
- [ ] 点击「苹果地图」按钮，跳转到苹果地图，目的地名称正确显示（中文不乱码）
- [ ] 点击「高德地图」按钮，跳转到高德地图，目的地名称正确显示
- [ ] 点击「百度地图」按钮，跳转到百度地图，目的地名称正确显示

## 5. 已知问题

- genchi 目录是测试项目，已添加到 .gitignore，不影响主项目运行
- 如果视频列表为空，说明该店铺关联的 author_restaurants 记录中 video_id 为空，这是正常的（老数据可能没有 video_id）

## 6. 后续优化建议

1. **视频封面图**：目前只显示博主头像，可以考虑从抖音 API 获取视频封面图
2. **视频标题**：可以在 author_restaurants 表中新增 video_title 字段，存储视频标题
3. **定位精度**：可以根据用户位置计算店铺距离，按距离排序显示
4. **缓存优化**：视频列表可以在前端缓存，避免重复请求
