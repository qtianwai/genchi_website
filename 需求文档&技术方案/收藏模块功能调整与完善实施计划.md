# 收藏模块功能调整与完善 — 实施计划

> 版本：v1.1 | 日期：2026-04-04 | 优先级：P1

## 背景与目标

当前 App 的「博主」和「收藏」分属两个独立 Tab，功能割裂。用户需要在不同页面间切换才能管理关注的博主和收藏的店铺。本次重构将两者合并为统一的「收藏」入口，同时新增博主详情页、店铺详情全屏页、店铺列表分组管理页，并引入避雷、删除、收藏理由等精细化数据操作，提升整体用户体验。

**关键约束：**
- "拉黑"概念全部改为"避雷"，前端所有显示文案均用"避雷"
- 均价字段不展示（目前拿不到数据）
- 地图筛选栏中"我的推荐"筛选 chip 需使用用户真实头像（非 icon）
- 收藏页店铺行：左滑操作为「收藏/取消收藏」「避雷」「删除」
- 收藏理由入口放在店铺卡片上（💬留言小图标），不放在左滑菜单；卡片上同时有📍导航图标
- 一个店铺只维护一条用户的收藏理由
- 收藏模块页面以 2026-04-04 截图为视觉基线，允许较大 UI 重构，但不减少现有能力
- MainTabView 不恢复底部居中 `+` Tab，添加店铺入口保留在地图页右上角
- 地图页继续保留底部快捷卡，不将点位点击改为直接跳转店铺详情页
- 收藏模块与地图页保持一致，始终使用全局固定日间浅色风格，不单独引入暗色主题

---

## 一、数据库变更

### 新增表（4张）

```sql
-- 1. 用户避雷店铺表
CREATE TABLE IF NOT EXISTS user_blocked_restaurants (
  id              uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id         uuid NOT NULL,
  restaurant_id   uuid NOT NULL REFERENCES restaurants(id) ON DELETE CASCADE,
  created_at      timestamptz DEFAULT now(),
  UNIQUE(user_id, restaurant_id)
);
CREATE INDEX IF NOT EXISTS idx_ubr_user ON user_blocked_restaurants(user_id);
ALTER TABLE user_blocked_restaurants ENABLE ROW LEVEL SECURITY;
CREATE POLICY "用户只能操作自己的避雷记录" ON user_blocked_restaurants FOR ALL USING (auth.uid() = user_id);

-- 2. 用户删除店铺表（全局隐藏）
CREATE TABLE IF NOT EXISTS user_deleted_restaurants (
  id              uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id         uuid NOT NULL,
  restaurant_id   uuid NOT NULL REFERENCES restaurants(id) ON DELETE CASCADE,
  created_at      timestamptz DEFAULT now(),
  UNIQUE(user_id, restaurant_id)
);
CREATE INDEX IF NOT EXISTS idx_udr_user ON user_deleted_restaurants(user_id);
ALTER TABLE user_deleted_restaurants ENABLE ROW LEVEL SECURITY;
CREATE POLICY "用户只能操作自己的删除记录" ON user_deleted_restaurants FOR ALL USING (auth.uid() = user_id);

-- 3. 用户自定义分组表
CREATE TABLE IF NOT EXISTS user_restaurant_groups (
  id          uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id     uuid NOT NULL,
  name        text NOT NULL,
  created_at  timestamptz DEFAULT now(),
  UNIQUE(user_id, name)
);
CREATE INDEX IF NOT EXISTS idx_urg_user ON user_restaurant_groups(user_id);
ALTER TABLE user_restaurant_groups ENABLE ROW LEVEL SECURITY;
CREATE POLICY "用户只能操作自己的分组" ON user_restaurant_groups FOR ALL USING (auth.uid() = user_id);

-- 4. 分组-店铺关联表
CREATE TABLE IF NOT EXISTS user_group_restaurants (
  id              uuid PRIMARY KEY DEFAULT uuid_generate_v4(),
  group_id        uuid NOT NULL REFERENCES user_restaurant_groups(id) ON DELETE CASCADE,
  restaurant_id   uuid NOT NULL REFERENCES restaurants(id) ON DELETE CASCADE,
  user_id         uuid NOT NULL,
  created_at      timestamptz DEFAULT now(),
  UNIQUE(group_id, restaurant_id)
);
CREATE INDEX IF NOT EXISTS idx_ugr_group ON user_group_restaurants(group_id);
ALTER TABLE user_group_restaurants ENABLE ROW LEVEL SECURITY;
CREATE POLICY "用户只能操作自己的分组店铺" ON user_group_restaurants FOR ALL USING (auth.uid() = user_id);
```

### 修改现有表

```sql
-- user_favorites 新增收藏理由字段
ALTER TABLE user_favorites ADD COLUMN IF NOT EXISTS note text;
```

---

## 二、后端 API（backend/main.py）

### 新增请求模型

```python
class AvoidRestaurantRequest(BaseModel):
    user_id: str
    restaurant_id: str

class DeleteRestaurantRequest(BaseModel):
    user_id: str
    restaurant_id: str

class UpdateFavoriteNoteRequest(BaseModel):
    user_id: str
    restaurant_id: str
    note: str

class CreateGroupRequest(BaseModel):
    user_id: str
    name: str

class AddToGroupRequest(BaseModel):
    user_id: str
    group_id: str
    restaurant_id: str
```

### 新增/修改路由

| 路由 | 方法 | 说明 |
|------|------|------|
| `/api/restaurants/avoid` | POST | 避雷店铺 |
| `/api/restaurants/unavoid` | POST | 取消避雷 |
| `/api/restaurants/avoided` | GET | 获取避雷列表 |
| `/api/restaurants/delete` | POST | 删除店铺（全局隐藏） |
| `/api/favorites/update-note` | POST | 更新收藏理由 |
| `/api/groups` | GET | 获取用户分组列表 |
| `/api/groups` | POST | 创建分组 |
| `/api/groups/{group_id}` | DELETE | 删除分组 |
| `/api/groups/{group_id}/restaurants` | POST | 添加店铺到分组 |
| `/api/groups/{group_id}/restaurants/{restaurant_id}` | DELETE | 从分组移除店铺 |
| `/api/authors/{author_id}/stats` | GET | 获取博主统计（餐厅数、粉丝数、城市数） |
| `/api/map/restaurants` | GET | **修改**：过滤已删除店铺，标记避雷店铺 |

---

## 三、iOS 文件变更

### 新增文件（`ios/FoodMap/genchi/genchi/` 下）

| 文件路径 | 职责 |
|---------|------|
| `Views/AuthorDetailView.swift` | 博主详情页（头像、统计、店铺列表、关注/取消关注） |
| `Views/RestaurantDetailView.swift` | 店铺详情全屏页（替代地图底部卡片，含所有操作） |
| `Views/RestaurantListView.swift` | 店铺列表页（系统预设分组 + 自定义分组管理） |
| `Views/GroupDetailView.swift` | 分组明细页（查看/管理分组内店铺） |

### 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `Views/MainTabView.swift` | 删除博主 Tab，Tab 结构调整为：地图、收藏、我的（管理员额外显示复核） |
| `Views/MapView.swift` | 添加按钮移至右上角；筛选栏默认隐藏，点击展开；"我的推荐" chip 使用用户真实头像 |
| `Views/FavoritesView.swift` | 完全重写：合并博主列表+收藏店铺，新增搜索、添加店铺入口，左滑操作 |
| `Models/Models.swift` | 新增 AvoidedRestaurant、RestaurantGroup、AuthorStats 模型；Favorite 新增 note 字段 |
| `Services/APIService.swift` | 新增避雷、删除、分组、收藏理由、博主统计等 API 方法 |

---

## 四、各页面详细 UI 设计

### 4.1 MainTabView（修改）

Tab 结构：
- Tab 1: 地图 (`map.fill`)
- Tab 2: 收藏 (`bookmark.fill`) ← 合并原博主+收藏功能
- Tab 3: 我的 (`person.circle.fill`)
- Tab 4: 复核（仅管理员可见）

### 4.2 MapView（修改）

**右上角按钮组**（垂直排列）：
- 添加店铺按钮（橙色圆形，44pt，`plus` 图标）→ 弹出添加菜单
- 筛选展开/收起按钮（毛玻璃背景圆形，44pt）→ 切换筛选栏显隐

**筛选栏**（默认隐藏，点击展开时从顶部滑入）：
- 博主筛选：全部、"我的推荐"（使用用户真实头像）、各博主头像
- 店铺分组筛选：全部、收藏、避雷

**店铺标注点击**：继续保留地图底部快捷卡作为主入口；收藏模块内部的店铺列表、博主详情、分组详情进入 RestaurantDetailView 全屏页

### 4.3 FavoritesView（完全重写）

**页面结构**：
```
NavigationStack {
    List {
        // 关注列表区域（Section Header: "关注的博主"）
        //   - 已关注博主列表（博主头像+昵称，副标题"抖音达人"，点击→AuthorDetailView）
        //   - 注意：不显示用户自己，用户通过左上角列表图标进入 RestaurantListView
        
        // 收藏店铺区域（Section Header: "收藏的店铺"）
        //   - 店铺卡片行（左滑：收藏/取消收藏、避雷、删除）
    }
    .navigationTitle("收藏")
    .toolbar {
        ToolbarItem(.navigationBarLeading) {
            NavigationLink → RestaurantListView（list.bullet 图标）
        }
        ToolbarItem(.navigationBarTrailing) {
            搜索按钮（magnifyingglass）
        }
    }
}
```

**关注列表行**：
- 圆形头像（44pt）+ 昵称 + 副标题（"抖音达人"）
- 不显示用户自己，用户通过左上角列表图标进入 RestaurantListView（店铺列表分组管理页）
- 点击博主 → AuthorDetailView（博主详情页）

**收藏店铺卡片**：
- 左侧：店铺图片（56pt 圆角）
- 中间：店铺名称 + 地址 + 分类标签；收藏理由（若有，灰色小字显示在名称下方）
- 右侧图标：💬留言图标（点击编辑收藏理由）+ 📍导航图标（打开地图导航）
- 左滑操作：
  - 橙色「收藏/取消收藏」按钮
  - 灰色「避雷」按钮
  - 红色「删除」按钮

### 4.4 AuthorDetailView（新增）

**页面说明**：从收藏页点击某个博主进入，展示博主完整信息和其推荐的所有店铺。

**布局**：
- 顶部信息卡片：博主头像（72pt 圆形）+ 昵称 + "抖音达人"标签 + 关注/取消关注按钮（胶囊形）
- 统计数据栏（三列）：餐厅数 | 粉丝数 | 城市数（灰色背景圆角卡片）
- 推荐店铺列表（LazyVStack）：复用店铺卡片行组件，支持左滑操作（收藏、避雷、删除）

**交互**：
- 关注状态切换：已关注→点击弹出 confirmationDialog 二次确认（"取消关注后，该博主推荐的店铺（已收藏店铺除外）将不再在地图上显示"）
- 未关注→点击直接关注
- 店铺行点击→跳转 RestaurantDetailView

### 4.5 RestaurantDetailView（新增，全屏页面）

**页面说明**：作为收藏模块内的统一店铺详情页，提供完整信息和操作入口。从收藏列表点击、博主详情店铺列表点击、分组内店铺点击进入。地图页仍保留底部快捷卡，不强制切换为直达详情。

**布局**：
- 顶部封面图（全宽，高度 220pt，AsyncImage 加载店铺图片）
- 店铺名称 + 分类标签（CategoryBadge）
- 地址行（mappin.and.ellipse 图标 + 地址文字）
- 操作按钮网格（2列 × 3行 LazyVGrid）：
  - 导航（蓝色）→ 打开地图导航
  - 收藏/已收藏（橙色）→ 切换收藏状态
  - 分享（灰色）→ 占位，后续实现
  - 分组（紫色）→ 弹出分组选择 Sheet
  - 避雷/已避雷（红色/灰色）→ 切换避雷状态
  - 删除（红色）→ 弹出二次确认 Alert
- 收藏理由区域（若已收藏）：显示/编辑收藏理由
- 关联博主推荐区域：展示推荐该店铺的博主视频列表

**交互**：
- 删除二次确认："删除后该店铺将不再显示在地图和列表中"
- 避雷切换即时生效，地图标注样式同步变化

### 4.6 RestaurantListView（新增）

**页面说明**：从收藏页点击"自己"进入，管理用户所有店铺的分组视图。

**布局**：
- 系统预设分组（不可编辑/删除）：
  - 全部店铺（`square.grid.2x2`，蓝色）
  - 收藏的店铺（`bookmark.fill`，橙色）
  - 避雷的店铺（`exclamationmark.shield.fill`，红色）
- 自定义分组区域（Section "我的分组"）：
  - 每行：文件夹图标 + 分组名 + 店铺数量
  - 左滑删除分组

**交互**：
- 右上角 `folder.badge.plus` 按钮 → 弹出创建分组 Sheet（输入分组名）
- 点击任意分组行 → 进入 GroupDetailView（展示该分组内的店铺列表）

### 4.7 GroupDetailView（新增）

**页面说明**：展示某个分组内的店铺列表，支持添加/移除店铺。

**布局**：
- 店铺列表（复用店铺卡片行组件）
- 左滑可从分组中移除
- 右上角添加按钮 → 弹出搜索 Sheet，从已有店铺中选择添加到分组

---

## 五、新增数据模型（Models.swift）

```swift
// 避雷记录
struct AvoidedRestaurant: Identifiable, Codable {
    let id: String
    let user_id: String
    let restaurant_id: String
    let created_at: String?
    let restaurants: Restaurant?
}

// 博主统计数据
struct AuthorStats: Codable {
    let restaurant_count: Int
    let follower_count: Int
    let city_count: Int
}

// 用户自定义分组
struct RestaurantGroup: Identifiable, Codable {
    let id: String
    let user_id: String
    let name: String
    let created_at: String?
    var restaurant_count: Int?
}

// Favorite 新增 note 字段（修改现有模型）
// 在现有 Favorite 结构体中添加：
//   let note: String?  // 收藏理由
```

---

## 六、新增 API 方法（APIService.swift）

```swift
// 博主统计
func getAuthorStats(authorId: String) async throws -> AuthorStats

// 避雷/取消避雷
func avoidRestaurant(userId: String, restaurantId: String) async throws
func unavoidRestaurant(userId: String, restaurantId: String) async throws
func getAvoidedRestaurants(userId: String) async throws -> [AvoidedRestaurant]

// 删除店铺（全局隐藏）
func deleteRestaurant(userId: String, restaurantId: String) async throws

// 收藏理由
func updateFavoriteNote(userId: String, restaurantId: String, note: String) async throws

// 分组管理
func getGroups(userId: String) async throws -> [RestaurantGroup]
func createGroup(userId: String, name: String) async throws -> RestaurantGroup
func deleteGroup(userId: String, groupId: String) async throws
func addToGroup(userId: String, groupId: String, restaurantId: String) async throws
func removeFromGroup(userId: String, groupId: String, restaurantId: String) async throws
```

---

## 七、实施顺序

### Phase 0：文档准备
1. ✅ 创建本实施计划文档

### Phase 1：数据库 + 后端（先做，iOS 依赖）
2. 执行数据库迁移 SQL（新增 4 张表，修改 user_favorites）
3. 在 main.py 新增避雷、删除、分组、收藏理由、博主统计 API
4. 修改 `/api/map/restaurants` 过滤已删除店铺、标记避雷店铺

### Phase 2：iOS 数据层
5. 更新 Models.swift（新增模型，修改 Favorite）
6. 更新 APIService.swift（新增所有 API 方法）

### Phase 3：iOS 新页面
7. 新增 AuthorDetailView.swift
8. 新增 RestaurantDetailView.swift
9. 新增 RestaurantListView.swift + GroupDetailView.swift

### Phase 4：iOS 现有页面改造
10. 重写 FavoritesView.swift（合并博主+收藏）
11. 修改 MapView.swift（添加按钮右上角，筛选栏默认隐藏，标注点击跳转全屏详情）
12. 修改 MainTabView.swift（删除博主 Tab）

### Phase 5：文档更新
13. 更新 supabase_schema.sql
14. 更新 README.md、产品功能清单.md、产品介绍.md

---

## 八、关键文件路径

- `ios/FoodMap/genchi/genchi/Views/MainTabView.swift`
- `ios/FoodMap/genchi/genchi/Views/MapView.swift`
- `ios/FoodMap/genchi/genchi/Views/FavoritesView.swift`
- `ios/FoodMap/genchi/genchi/Views/AuthorsView.swift`（将被删除或替换）
- `ios/FoodMap/genchi/genchi/Models/Models.swift`
- `ios/FoodMap/genchi/genchi/Services/APIService.swift`
- `backend/main.py`
- `backend/supabase_schema.sql`

---

## 九、验证方式

1. **数据库**：确认 4 张新表已创建，user_favorites 有 note 字段
2. **后端**：用 curl 测试各新接口返回正确
3. **iOS**：
   - Tab 栏为地图、收藏、我的；管理员额外显示复核
   - 地图右上角有添加和筛选按钮，筛选栏默认隐藏
   - 收藏页展示博主列表 + 收藏店铺，左滑有操作
   - 点击博主进入博主详情页，有统计数据和关注/取消关注
   - 点击店铺进入全屏详情页，有导航/收藏/避雷/删除/分组操作
   - 店铺列表页有系统分组 + 自定义分组
   - 取消关注/删除店铺有二次确认弹窗
   - 收藏理由通过卡片上留言图标编辑

---

## 十、Bug 修复记录（v5.0.1）

> 日期：2026-04-04

### 修复清单

| # | 问题描述 | 修复方案 | 涉及文件 |
|---|---------|---------|---------|
| 1 | 收藏页左上角缺少返回按钮，无法跳转到店铺列表页 | 在 FavoritesView toolbar 左侧新增 NavigationLink（`list.bullet` 图标），点击跳转 RestaurantListView | `FavoritesView.swift` |
| 2 | 收藏页关注列表中显示了用户自己 | 移除 FavoritesView 中"自己"那一行（NavigationLink → RestaurantListView），关注列表仅展示已关注博主 | `FavoritesView.swift` |
| 3 | 店铺列表页"全部店铺"统计数据错误（仅统计收藏+避雷） | 改为调用 `getMapRestaurants` 接口，统计地图上所有可见店铺（博主推荐 + 用户自建推荐去重）；同步修改 GroupRestaurantListView 的 `.all` 分支 | `RestaurantListView.swift` |
| 4 | 地图底部卡片收藏标识不同步（已收藏店铺显示未收藏） | RestaurantCard 的 `.task(id:)` 中新增 `checkFavoriteStatus()` 调用，每次切换店铺时从 API 获取真实收藏状态 | `MapView.swift` |
| 5 | 店铺列表页点击"收藏的店铺"进入了独立列表页而非收藏页 | 将"收藏的店铺"从 NavigationLink 改为 Button + `dismiss()`，直接返回收藏页（收藏页本身已展示收藏店铺列表） | `RestaurantListView.swift` |

### 设计调整说明

- **FavoritesView 页面结构变更**：移除"自己"入口后，用户通过左上角列表图标进入店铺列表页管理分组，关注列表仅展示已关注的博主
- **RestaurantListView 导航逻辑变更**："收藏的店铺"不再打开独立子页面，而是 dismiss 回到收藏页，因为收藏页下半部分已经是完整的收藏店铺列表
- **全部店铺统计口径变更**：从"收藏+避雷去重"改为"地图上所有可见店铺"，与用户在地图页看到的数据一致
